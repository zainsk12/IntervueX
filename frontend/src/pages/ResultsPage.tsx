import { PlaceholderPage } from '../components/PlaceholderPage'
import { ROUTES } from '../data/routes'

export default function ResultsPage() {
  return (
    <PlaceholderPage
      title="Results"
      description="Final assessment synthesized from interview evidence."
      path={ROUTES.RESULTS}
    />
  )
}
