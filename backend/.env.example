# Server
PORT=4000
NODE_ENV=development

# CORS — the frontend dev server origin (Vite default)
CORS_ORIGIN=http://localhost:5173

# LLM providers — NOT required for Phase 1 (Backend Foundation).
# Added here now so later phases only need to fill these in.
GROQ_API_KEY=
MISTRAL_API_KEY=
